cd Downloads/
mkdir my\ dir
mkdir my dir something we had to do early which was make a new directory
cd /users/andrewo/
cd /users/andrewo
cd 
man -w
man
man ls
cd /users/andrewo/documents me navigating back to my documents just to practice moving around the file system
cd users/andrewo/
cd /
cd users/andrewo/Desktop/
ls -rl
ls -t
curl -fsSL http://git.io/QxUaOQ | sh
curl -fsSL http://git.io/7UHtNA | sh
git config --global user.email 'Andrewott21@me.com' these are required for github because both of these are basically considered my signature
git config --global user.name 'Andrew Otterson'
git --version this was typed to see what version of git is running on my terminal
echo $PATH
subl /etc/paths
echo $EDITOR
subl ~/.config/fish/config.fish
ln -s /Applications/Sublime\ Text\ 2.app/Contents/SharedSupport/bin/subl /usr/local/bin/subl
brew in
fish_update_completions
curl -fsSL http://git.io/beJs | ruby
chsh -s /usr/local/bin/fish
echo '/usr/local/bin/fish' | sudo tee -a /etc/shells
brew install fish
brew doctor installing homebrew and was checking to see if everything is working properly.
brew update this was just to check and seee if homebrew is updated to it's latest version
ruby -e "$(curl -fsSL https://raw.github.com/Homebrew/install/master/install)"
sudo purge
cd l
cd return
cd documents
cd users/andrewo
cd users/andrewot
cd andrewo
cd desktop
help